syntax = "proto3";

import "nanopb.proto";


message uartMessage {
  bytes mac = 1 [(nanopb).max_size = 6];
  bytes ssid = 2 [(nanopb).max_size = 33];
  uint32 channel = 3;
  int32 rssi = 4;
  int32 authmode = 5;
  int32 pairwise_cipher = 6;
  int32 groupwise_cipher = 7;
  bytes country = 8 [(nanopb).max_size = 3];
}